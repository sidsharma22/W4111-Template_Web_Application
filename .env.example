APP_NAME=W4111 Web Application

# MySQL connection settings (used by MySQLDataService)
MYSQL_HOST=localhost
MYSQL_PORT=3307
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=classicmodels
# Optionally use a Unix socket instead of TCP (leave blank to use host/port):
# MYSQL_UNIX_SOCKET=/tmp/mysql_custom.sock

# Server settings
HOST=0.0.0.0
PORT=8000
